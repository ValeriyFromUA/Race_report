parameters:
  - name: driver
    description: 'returning one driver`s static'
    in: path
    type: string
    enum: [ 'Fernando Alonso', 'Valtteri Bottas' ]
    required: false
    default: 'Fernando Alonso'
responses:
  200:
    description: 'returning one driver`s static'
    schema:
      $ref: '/api/v2/report/order=asc/driver=<string:driver>'
