parameters:
  - name: order
    description: 'return static in "asc" order by default'
    in: PATH
    default: 'asc'
responses:
  200:
    description: 'return static in "asc" order by default'
    schema:
      $ref: '/api/v2/report'
