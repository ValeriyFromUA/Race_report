parameters:
  - name: order
    description: 'return static in "asc" or "desc" order'
    in: PATH
    type: string
    enum: [ 'asc', 'desc' ]
    required: true
    default: 'asc'
responses:
  200:
    description: 'return static in "asc" or "desc" order'
    schema:
      $ref: '/api/v2/report'
